# Tópicos Importantes

- Qual a linguagem de programação utilizada na disciplina?React Native

- O que é o Node?ambiente que executa o js

- O que é Framework e qual a diferença para uma biblioteca?o frmawork te orienta melhor para o seu uso ja a biblioteca não

- O que é o NPM? node package menager, gerenciador de pacotes do node 

- O que é React? é uma biblioteca js

- O que é o React Native? é um framework que permite criar aplicativos mobile, js

- O que é o Expo?ferramenta do reactNative

- Como estilizar um componente no React Native?é inpirado no css e vc usa {Style}

- O estilo CSS in JS no React Native é igual ao CSS Web? Tenho todas as propriedades?

- Em quais pacotes encontro um componente de Imagem? Qual melhor para usarmos atualmente?

- O que são propriedades de componentes?

- O que são Hooks?são funções do react q nem no html vem antes do return pricipais {useState, useEffect}

-Para que serve e como funciona o useState?para atualizar um estado(componente)

-Para que serve o useEffect? é um hook que funciona como gatilho e toda vez que um dos componentes que ela ta observando muda ela dispara uma função 
Extra

- Revisão de variáveis e objetos
    - desestruturação de objetos

- O que é Prop Drilling? é uma tecnica do React onde um componente pai passa uma ou mais constate(const) para o filho. passa uma const para outra pagina sem precisar repeti-la .

## Comandos Uteis

// Como instalar o React Native com Expo?

npx create-expo-app@latest --template

### Expo


